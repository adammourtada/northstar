# Variable names only; these integrations are planned and not configured yet.
# Store real values in ignored local environment files or in your hosting
# provider's secret management. Never commit credentials or populated env files.
# Service-role, database, and AI credentials must remain server-side.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
DATABASE_URL=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
